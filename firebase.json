{
  "storage": {
    "rules": "storage.rules",
    "cors": [{
      "origin": ["http://localhost:3005", "https://your-production-domain.com"],
      "method": ["GET", "POST", "PUT", "DELETE", "HEAD"],
      "maxAgeSeconds": 3600,
      "responseHeader": ["Content-Type"]
    }]
  }
}
